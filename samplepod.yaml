metadata:
  labels:
    app: my-pod
spec:
  containers:
  - name: my-pod
    image: mysql
    ports:
      - name: http
        protocol: TCP
        containerPort: 80
    env:
    - name: "MYSQL_ROOT_PASSWORD"
      value

apiVersion: v1
kind: Pod